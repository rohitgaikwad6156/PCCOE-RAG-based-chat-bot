{
  "name": "rag-college-chatbot-root",
  "version": "1.0.0",
  "description": "Production-ready full-stack RAG-Based College Chatbot with semantic search, document ingestion, and admin management",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:client\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:client": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "seed:admin": "cd backend && npm run seed:admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
